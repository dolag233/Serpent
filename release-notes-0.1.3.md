# Serpent 0.1.3 发布说明

> 变更范围：v0.1.2..HEAD（2026-08-19 → 2026-08-21）

## 新增功能

- **NAS/网络共享资源库（实验性）**：支持在网络共享（NAS/SMB）上打开与创建资源库，自动使用回滚日志模式；文件锁与断线恢复取决于 NAS，请保持同一时间只有一个实例写入并做好备份（`Serpent-4f44f1` 系列）
- **自动更新**：接入 GitHub Release 更新流——检查/下载/取消/进度展示；Windows 安装流程收口（portable 更新保留下载包，安装型更新自动清理临时文件）
- **查看器 Ctrl+C 复制**：资产查看界面支持 Ctrl+C 复制资产；视频查看器右键菜单新增复制（`Serpent-54598c` 系列）
- **PDF 查看器缩放/平移**：滚轮缩放锚定鼠标位置、拖拽抓手、缩放渲染清晰（`Serpent-d14dd2` 部分）
- **视频代理播放**：已生成代理的视频在卡片 hover 与查看界面播放代理视频；源播放失败时静默降级生成代理（`Serpent-c8a1a3`）
- **主菜单「检查更新」**：关于菜单不再显示版本号，改为「检查更新」入口（打开关于页处理自动更新）

## 修复

- 视频查看界面代理状态三态区分：仅「生成失败」显示警告，加载中/生成中为普通状态提示（`Serpent-e56a1f`）
- 视频代理提示随鼠标停驻 UI 一并渐隐，不再遮挡画面（`Serpent-d259bc`）
- NAS 与恢复提示弃用常驻 banner——NAS 提示改为 warning toast，恢复提示改为确认弹窗（含「查看恢复报告」入口）
- PDF 白屏/闪烁：渲染完成再替换占位、缩放时旧节点作过渡占位、缩放中心精确锚定
- 递归显示子文件夹时不再显示子文件夹卡片图标
- 合集浏览性能优化（递归合集浏览）
- Windows 资源库切换时的库文件锁问题记录与修复

## 已知限制

- NAS 资源库为实验性支持：文件锁与断线恢复取决于共享盘实现；同一资源库仅支持单实例写入，多机同时写暂不支持（详见 `docs/internal/research/2026-08-19-nas-single-writer-multi-reader-decision.md`）
