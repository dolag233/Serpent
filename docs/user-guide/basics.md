# 基本使用

## 创建资源库

首次启动显示创建对话框：

1. 选择存放位置
2. 输入名称，点击「创建」

资源库目录包含 `Assets/` 与 `.serpent/`。在「资源库菜单」中可以创建、打开或切换资源库。

## 导入资产

支持图片、视频、音频、3D 模型（FBX/OBJ/GLB/STL 等）、文本、RAW 等格式。

- 把文件或文件夹拖入窗口（文件夹递归导入）
- 点击「导入文件」或「导入文件夹」
- 浏览器扩展：网页图片/视频右键保存或拖拽导入

导入时文件复制到资源库 `Assets/` 目录，建立稳定的资产身份。同名或内容重复的文件会弹出冲突对话框，选择处理方式即可。

## 浏览

- 左侧栏：所有资产、回收站、标签管理、文件夹、合集、智能合集
- 顶部工具栏：格式过滤（图像/视频/音频/3D/文本）、尺寸过滤、排序
- 卡片显示缩略图与文件名。悬停预览，单击选中，双击打开查看器
- 框选：按住拖拽；多选：⌘/Ctrl+单击。多选后在 Inspector 批量操作（评分、标签等）

## 搜索

顶部搜索框支持关键字搜索。可搜索字段：文件名、标签、描述、来源 URL、作者、文件夹路径、元数据。点击搜索框旁的 `?` 查看语法。搜索可与格式、尺寸、评分、标签筛选组合。

## 标签与合集

- 标签：Inspector 或右键菜单添加；「标签管理」视图统一管理
- 合集：手动合集拖拽资产加入；智能合集按搜索条件自动聚合，条件变化结果自动更新

## Inspector

选中资产后右侧面板显示基本信息（文件名、大小、尺寸、修改时间、路径）、标签、评分、收藏、描述、来源链接、作者、技术信息（格式、编解码、色彩空间）与 AI 分析内容（如可用）。

## 3D 查看器

双击 3D 模型打开。

- 左键旋转，右键平移，滚轮缩放（中键 dolly）
- 工具栏：HDRI 环境光切换（内置 4 套 Poly Haven 预设）、光照强度、预览模式、显示模式
- FBX 经内置转换管线（ufbx → GLB）支持 PBR 材质

## 回收站

删除的资产进入回收站，保留 30 天，可恢复或彻底删除。文件夹与合集支持类似操作。

## 快捷键

| 操作 | macOS | Windows |
| --- | --- | --- |
| 打开（查看器） | Enter | Enter |
| 外部程序打开 | ⌘O | Ctrl+O |
| 在文件夹中显示 | ⌘⇧S | Ctrl+Shift+S |
| 搜索聚焦 | ⌘F | Ctrl+F |
| 重命名 | F2 | F2 |
| 移到回收站 | ⌘⌫ | Delete |
| 从磁盘删除 | ⌥⌘Delete | — |
| 复制 / 粘贴 | ⌘C / ⌘V | Ctrl+C / Ctrl+V |
