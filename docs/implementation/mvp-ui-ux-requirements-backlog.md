# Serpent MVP UI/UX 与文件管理需求池

> 状态：生效，需求继续收集中
> 建立：2026-07-16
> 适用版本：除明确标记为 MVP 后的条目外，均进入 v0.1.0 MVP 排期
> 来源：2026-07-16 产品负责人两轮真实使用反馈
> 工单系统：本池待办已开为 beads 工单（`bd ready --json` 查看就绪队列），执行状态以 bd 为准；本文件保留需求来源、用户原话与验收记录。

## 用途

本文是 0013/0014 之后新增 UI、UX、浏览、文件夹、标签、国际化与主题需求的共享入口。
后续 agent 开始相关工作前必须先阅读本文，并在实施时把条目拆入独立垂直切片规格、开发日志、审查和 QA 文档。

本文不是“已经实现”清单。除非 `docs/qa/human-acceptance-checklist.md` 中存在独立的待验收条目，否则本文中的需求都不得向用户宣称可验收或已完成。

## 已确认产品方向

1. UI/UX 当前以 Eagle 的信息密度、控件分组和美术工作流为主要方向参考，但不复制其品牌和每个像素。
2. 浏览、导航、过滤、文件夹和 Inspector 是 MVP 核心工作流，不推迟到所有底层功能完成以后再处理。
3. 资产只使用真实文件名作为名称；撤销独立 Label/显示别名产品概念，详见 ADR 0022。
4. 标签主要用于过滤与资产元数据，不在左侧导航枚举全部标签，也不建立独立标签管理页面。
5. 中文和英文、亮色和暗色主题进入 MVP；自定义主题推迟，但 MVP 的 token 架构不得阻塞后续扩展。
6. 普通文件夹与链接文件夹在导航中使用同一棵目录结构呈现；链接性质和离线状态通过图标、颜色与 tooltip 表达。
7. 右键菜单随单项、多选、文件夹、回收站和链接状态变化，但共享同一套视觉组件、命令定义、快捷键和危险操作语义。

## 需求清单

### A. 应用壳层与导航

| ID | 需求 | MVP | 状态 |
| --- | --- | --- | --- |
| REQ-SHELL-001 | 资产浏览画布使用干净纯色背景，移除一格一格的装饰图案。 | 是 | 已实现 `38fa873`；人类验收通过（SHELL-001） |
| REQ-SHELL-002 | 修复全局工具栏、浏览工具栏、目录名称与数量等控件的竖直对齐。 | 是 | 部分实现 `591f524`；仍需统一壳层收口 |
| REQ-SHELL-003 | 移除右下角 `SERPENT / LOCAL WORKSPACE` 装饰文字。 | 是 | 已实现 `38fa873`；人类验收通过（SHELL-002） |
| REQ-SHELL-004 | 移除导航底部“本地资源库 · 已连接”等显而易见状态。 | 是 | 已实现 `38fa873`；人类验收通过（SHELL-002） |
| REQ-SHELL-005 | 左上角不显示 Logo + Serpent，改为当前资源库名称和资源库下拉菜单；菜单承载切换及资源库级操作。 | 是 | 已实现 0016-A；待人类验收（SHELL-004） |
| REQ-SHELL-006 | 移除“资源导航”“检查器”等面板名称，内容本身承担语义。 | 是 | 已实现 `38fa873`；人类验收通过（SHELL-002） |
| REQ-NAV-001 | 顶部目录导航使用无边框面包屑；不显示无效的起始“资源库 >”。 | 是 | 已实现 0016-A；人类验收通过（NAV-001） |
| REQ-NAV-002 | 面包屑使用 `>` 表示层级，点击任一父目录直接跳转。 | 是 | 已实现 0016-A；人类验收通过（NAV-001） |
| REQ-NAV-003 | 提供工作区历史后退/前进，恢复此前的浏览范围；它不是撤销/重做，也不同于查看页面的“返回”。 | 是 | 已实现 0016-A（范围 identity，不含滚动/查看页）；人类验收通过（NAV-002） |
| REQ-NAV-004 | 普通文件夹与链接文件夹不分区展示；在线链接不显示专用链接图标（与托管同为文件夹图标）；离线链接使用灰色断链图标，hover 解释状态。 | 是 | 已按 Serpent-6nb 调整（2026-07-18）；待人类验收（NAV-003 / AVAIL-001） |
| REQ-VIEW-001 | 查看页面移除“图像预览”等无效媒体类型小字。 | 是 | 已实现 2026-07-17（Wave 3 审查顺手收口，截图确认）；待人类验收（VIEWER-002）；查看页整体重设计仍属 0013 |
| REQ-SHELL-007 | 左侧导航与右侧 Inspector 面板支持拖拽调宽，宽度跨完整重启持久化。 | 是 | 已实现（Wave 2 T5，版本化偏好+拖拽 hook）；待人类验收（SHELL-009） |
| REQ-SHELL-008 | 新建资源库界面移除步骤侧边栏与「01」装饰，直接呈现表单。 | 是 | 已实现（Wave 1 T1）；待人类验收（SHELL-007） |
| REQ-SHELL-009 | 移除装饰性冗余文案（如新建文件夹对话框中文环境下的英文「managed folder」行）；界面只保留功能性文字。 | 是 | 已实现（Wave 1 T1+T4，文件夹对话框整体移除）；待人类验收（SHELL-007） |
| REQ-SHELL-010 | 右下角通知自动关闭时使用淡出过渡；核心界面状态变化逐步补充克制、统一的动效。 | 是 | 已实现淡出（Wave 1 T1，含状态机单测）；待人类验收（SHELL-008）；更多动效后续增量 |
| REQ-NAV-005 | 目录树选中/悬停高亮只改变背景色，不使用边缘强调色描边或竖条。 | 是 | 已实现（Wave 1 T1）；待人类验收（NAV-004） |

### B. 资产画布与发现工具栏

| ID | 需求 | MVP | 状态 |
| --- | --- | --- | --- |
| REQ-CANVAS-001 | “缩略图大小”不显示冗余文字，只保留滑块；仍保留 tooltip 和无障碍名称。 | 是 | 已实现 `38fa873`；人类验收通过（SHELL-003） |
| REQ-CANVAS-002 | 浏览工具栏只承载与浏览相关的搜索、过滤、排序、视图和显示控制。 | 是 | 待实施 |
| REQ-CANVAS-003 | 导入文件、导入文件夹、粘贴图片、链接导入和资源库导入导出移出常驻浏览工具栏，迁入资源库菜单或统一添加入口；拖放与快捷键保留。 | 是 | 待规格化 |
| REQ-FILTER-001 | 把当前长表单过滤改为美术友好的维度过滤条，优先展示颜色、标签、文件夹、形状/长宽比、评分、格式和扩展入口。 | 是 | **部分实现**（2026-07-18：`Serpent-fqt` 第一垂直切片落地标签/形状/评分/格式/更多；颜色与文件夹维度未挂）；待人类验收 FILTER-013 |
| REQ-FILTER-002 | 已启用条件在工具栏中持续可见，可逐项删除或全部清除；同字段 OR、跨字段 AND、排除条件等现有语义保持。 | 是 | **部分实现**（2026-07-18：激活 chips + 清除）；待人类验收 FILTER-014 |
| REQ-CANVAS-004 | 小卡片和瀑布流必须充分使用可用宽度，不允许右侧出现可容纳多列的大面积空当。 | 是 | 已实现显式瀑布列与平铺弹性列；瀑布流已由用户验收，平铺仍待复验 |
| REQ-CANVAS-005 | 修改日期的显示开关使用秒表/时间图标，不使用五角星。 | 是 | 已合入 `5b8b8fe`；待用户验收 |
| REQ-CANVAS-006 | AI 搜索按钮使用正确图标且文字不溢出按钮；关键词搜索输入框加宽到合理可用宽度。 | 是 | 已实现（Wave 1 T1）；待人类验收（CANVAS-011） |
| REQ-CANVAS-007 | 卡片尺寸滑块整体更小巧（轨道更细、thumb 更小），并使用中性色而非强调色。 | 是 | 已实现（Wave 1 T1）；待人类验收（THEME-001） |
| REQ-CANVAS-008 | 资产卡片预览图四角统一圆角（上边、下边都圆角）。 | 是 | 已实现（Wave 1 T1）；待人类验收（CANVAS-010） |
| REQ-FILTER-009 | 宽高比过滤提供常用预设（16:9 / 4:3 / 1:1 / 3:4 / 9:16），按最接近比例匹配（±5% 相对容差），保留自定义输入。 | 是 | 已实现（Wave 2 T7，预设 chips）；待人类验收（FILTER-010） |
| REQ-FILTER-010 | 分辨率过滤提供 1K / 2K / 4K 预设（按长边分桶），可自定义输入。 | 是 | 已实现（Wave 2 T7，新 long_edge 查询字段+chips）；待人类验收（FILTER-011） |

### C. Inspector 与状态连续性

| ID | 需求 | MVP | 状态 |
| --- | --- | --- | --- |
| REQ-INSPECT-001 | Inspector 对可预览资产显示真实缩略图或视频封面；未生成时复用自动预览队列，不支持时显示格式图标。 | 是 | 已实现真实 artifact 解码与等比宽度优先布局；等比无框轻圆角已由用户验收 |
| REQ-INSPECT-002 | 从一个资产切换到另一个资产时，Inspector 不闪空、不显示前后两个资产的混合状态。 | 是 | 已实现身份守卫并移除连接/加载占位；Electron E2E 记录切换全程，待用户复验 |
| REQ-INSPECT-003 | Inspector 不显示面板名称，基本信息采用紧凑、稳定且与当前资产一致的布局。 | 是 | 已实现大图、文件名、紧凑元数据和 tag chip；继续随 UX 反馈打磨 |

### D. 文件夹卡片、范围与计数

| ID | 需求 | MVP | 状态 |
| --- | --- | --- | --- |
| REQ-FOLDER-001 | 进入文件夹后，资产画布在资产之前或按确定规则显示其直接子文件夹卡片。 | 是 | 待规格化 |
| REQ-FOLDER-002 | 文件夹卡片使用一至三张代表性资产缩略图形成类似 Windows 图片文件夹的内容预览。 | 是 | 待规格化 |
| REQ-FOLDER-003 | 所有显示目录的位置都显示资产数量，包括导航树、浏览区文件夹卡片和选择目标对话框。 | 是 | 待规格化 |
| REQ-FOLDER-004 | 文件夹范围可显式切换是否包含后代文件夹资产；设置的作用域与持久化方式待确认。 | 是 | 待规格化 |
| REQ-FOLDER-005 | 文件夹封面和计数必须批量获得，禁止每文件夹一次 IPC/SQL 的 N+1 实现。 | 是 | 工程约束 |
| REQ-FOLDER-006 | 资产浏览模型显式区分 `folder` 与 `asset`，不能伪造文件夹资产；搜索、合集等非目录范围不得错误混入文件夹卡片。 | 是 | 工程约束 |
| REQ-FOLDER-007 | 文件夹「新建子文件夹」与「重命名」在目录树内原地编辑完成，不使用弹出对话框；侧栏「+」入口走同一原地编辑流程。 | 是 | 已实现（Wave 1 T4，状态机+单测）；待人类验收（MENU-019） |

### E. 资产、文件夹右键菜单与快捷键

| ID | 需求 | MVP | 状态 |
| --- | --- | --- | --- |
| REQ-MENU-001 | 指针进入菜单项时只显示一套高亮；保留键盘导航能力，但不能同时出现焦点色和 hover 色。 | 是 | 已实现指针/键盘输入模式切换与 E2E；待用户复验 |
| REQ-MENU-002 | 单资产至少支持默认应用打开、其他应用打开、在 Finder/Explorer 显示、复制、粘贴、重命名、复制文件路径、移至回收站和永久删除。 | 是 | 部分实现：默认应用打开（既有）、在 Finder/Explorer 显示与复制文件路径（2026-07-17）、重命名真实文件（2026-07-17 第二增量，含 Windows 禁用字符门禁，MENU-015 人类验收通过）；其他应用打开/复制/粘贴仍缺口 |
| REQ-MENU-003 | 资产菜单按“打开”“剪贴板/组织”“元数据”“删除”等同类操作分组，不同组使用分隔。 | 是 | 已实现 `197ea9e`；人类验收通过（MENU-014）；完整动作集合仍按 0017 |
| REQ-MENU-004 | 多选菜单按选择内容、位置类型、可用性和回收站状态生成，显示处理数量、跳过数量与原因。 | 是 | 需扩展 0014 |
| REQ-MENU-005 | 文件夹菜单至少支持在当前目录新增子文件夹、重命名、复制、粘贴、克隆、移动和删除，并与资产菜单共享组件与视觉。 | 是 | 部分实现：托管文件夹新建子文件夹 + 重命名真实目录（2026-07-17 第三增量，共享 ContextMenu 组件，MENU-016/017 人类验收通过）；复制/粘贴/克隆/移动/删除待澄清队列 #5/#7 |
| REQ-MENU-006 | 文件夹右键菜单支持「复制文件夹路径」与「在 Finder/Explorer 中打开」，与资产命令同一安全链路（Renderer 只传 folder ID，绝对路径不越界）。 | 是 | 已实现（Wave 1 T2，全链路+worker 7/7）；待人类验收（MENU-018） |
| REQ-COMMAND-001 | 建立平台感知的统一命令注册表，由同一定义提供文字、图标、快捷键、可用性、禁用原因和执行函数。 | 是 | 部分实现（0015-A/B/C，2026-07-17）：核心注册表 `src/renderer/commands/`；资产单选/多选/文件夹/合集/智能合集五个菜单表面全部接入（30 条定义、114+ 单测、零布局重排、E2E 全绿）；快捷键定义与事件匹配已统一（ShortcutChord + matchesShortcut，旧 asset-command-shortcuts 双份定义删除）；工具栏按钮接入与快捷键补全待后续 |
| REQ-COMMAND-002 | 常用菜单项显示真实可用的 macOS/Windows 快捷键；输入框、对话框和媒体控件聚焦时避免冲突。 | 是 | 部分实现 `e2d5d60` + `5b8b8fe`；已实现路径人类验收通过（COMMAND-001）；2026-07-17 快捷键展示与事件匹配已统一进注册表 ShortcutChord（单一定义双用途，防漂移）；重命名等更多命令快捷键仍待补 |
| REQ-COMMAND-003 | “复制路径”“其他应用打开”“在文件浏览器显示”等操作只以资产 ID 跨 Renderer 边界，绝对路径由 Main/Worker 安全处理。 | 是 | 架构约束 |

### F. 选择模型

| ID | 需求 | MVP | 状态 |
| --- | --- | --- | --- |
| REQ-SELECT-001 | 空白拖动框选支持 Shift、Windows Ctrl、macOS Command 及组合键语义，而不只支持点击组合键。 | 是 | 已合入 `5b8b8fe` 并有 E2E；macOS 待用户复验，Windows Ctrl 待平台验证 |
| REQ-SELECT-002 | 所有多选入口复用同一选择 reducer/状态机，视图切换、文件夹卡片、搜索结果和连续加载不产生不同组合键规则。 | 是 | 工程约束 |
| REQ-SELECT-003 | 资产选中描边向外扩展并略粗于预览图自身描边，四边完整可见、不被预览图遮挡；消除选中态按住 Shift 悬停已选资产时出现的第二圈描边。 | 是 | 已实现（Wave 1 T1，外扩 2px 环 + :focus-visible 双圈根因修复）；待人类验收（SELECT-008） |

### G. 标签与 Label 退役

| ID | 需求 | MVP | 状态 |
| --- | --- | --- | --- |
| REQ-TAG-001 | 左侧导航不枚举全部标签，不建立独立标签管理页面。 | 是 | 已实现 2026-07-17 第二增量（侧栏标签区与新建入口移除，shell-navigation 负向断言防回归）；标签重命名/删除暂无 UI 入口，待澄清队列 #8 裁决 |
| REQ-TAG-002 | 标签作为过滤维度和资产元数据；过滤入口支持大规模标签的搜索与计数，不直接铺开全部标签；支持同时选择多个标签进行过滤。 | 是 | 已实现多标签选择器（Wave 2 T7：搜索+计数+chips 多选，同字段 OR）；待人类验收（FILTER-009） |
| REQ-TAG-003 | Inspector 用 tag chip 显示和移除标签；添加时显示最近标签、搜索现有标签，并可输入新名称创建后分配。 | 是 | 已实现并由用户验收；零使用标签不进入建议，点击或键盘确认立即分配 |
| REQ-TAG-004 | 右键菜单中的标签操作不得为每个标签生成一行；使用可搜索的二级选择器。 | 是 | 已实现 2026-07-17（单资产添加、批量添加/移除、搜索/计数/键盘导航、菜单内滚动豁免）；单测与 E2E 全绿。2026-07-17 用户试用后反馈"有点难用"、需要多标签过滤，该方向由用户另行规划（见 REQ-TAG-002 冻结注记），TAG-004/TAG-005 保持验收不通过；2026-07-17 第二批反馈中用户已直接给出多标签过滤方向，冻结解除（见 REQ-TAG-002） |
| REQ-LABEL-001 | 产品、UI、搜索、AI 和一等客户端不再提供独立 Label/资产显示别名。 | 是 | 已确认；v14 迁移与协议/AI/搜索退役已合入 `5b8b8fe`，macOS 开发态 QA 有条件通过 |
| REQ-LABEL-002 | 卡片、Inspector 和搜索统一使用真实文件名；修改显示名称等同于重命名文件，必须走文件操作语义。 | 是 | 已确认；Label 退役已实现；重命名真实文件 2026-07-17 第二增量已实现（asset.renameFile，扩展名保留、冲突/非法名类型化拒绝，MENU-015 人类验收通过） |
| REQ-LABEL-003 | 旧 Label 数据和旧智能合集查询的迁移方式在编码前确认，不允许静默丢弃用户数据。 | 是 | 已确认：预发布旧 Label 值直接丢弃；含 Label 条件的旧智能合集删除；其余元数据和智能合集保留 |

### H. 多语言与主题

| ID | 需求 | MVP | 状态 |
| --- | --- | --- | --- |
| REQ-I18N-001 | 首发 UI 至少支持简体中文和英文，界面文字、菜单、对话框及安全错误原因使用统一翻译键。 | 是 | 已实现；人类验收通过（2026-07-18）；默认跟随系统语言 |
| REQ-I18N-002 | 持久日志保留稳定错误码和诊断，不依赖本地化文字做机器判断。 | 是 | 工程约束 |
| REQ-THEME-001 | 首发支持亮色、暗色和跟随系统，偏好跨完整重启保存。 | 是 | 已实现；THEME-002 人类验收通过（2026-07-18）；默认主题 interim dark（#11 主题侧未裁决） |
| REQ-THEME-002 | 所有核心界面改用语义设计 token，禁止为亮色主题复制一套散落的硬编码颜色。 | 是 | 已实现；随 THEME-002 人类验收通过（2026-07-18） |
| REQ-THEME-003 | 自定义主题推迟到 MVP 后；MVP token 设计需允许后续扩展。 | 否 | 已排期 MVP 后 |
| REQ-THEME-004 | 默认强调色由绿色改为蓝色（token 一处修改全局生效）；强调色不再用于卡片尺寸滑块等连续控件。 | 是 | 已实现（Wave 1 T1，#3b82f6 + color-mix 派生）；待人类验收（THEME-001） |

### I. 拖拽与直接操作（2026-07-17 第二批新增）

| ID | 需求 | MVP | 状态 |
| --- | --- | --- | --- |
| REQ-DND-001 | 资产可拖拽到目录树中的文件夹完成移动；托管资产物理移动并事务更新路径，链接资产或不可用目标给出明确说明。 | 是 | 已实现（Wave 2 T6：复用 moveAssets，快照/跳过/同目录 no-op 提示）；待人类验收（DND-001） |
| REQ-DND-002 | 资产可拖拽到回收站完成移入回收站，语义与现有回收站命令一致（含数量确认）。 | 是 | 已实现（Wave 2 T6：复用 trashManagedAssets）；待人类验收（DND-002） |

### J. 已知缺陷（2026-07-17 第二批新增）

| ID | 缺陷 | MVP | 状态 |
| --- | --- | --- | --- |
| BUG-TRASH-001 | 资产移入回收站后预览图丢失；回收站内应继续显示可解码的预览。 | 是 | 已修复（`39f134d`，根因：artifact 解析 SQL 误过滤 deleted_at；worker 级四列齐）；E2E/视觉证据待补，TRASH-004 待人类验收 |

### K. 递归范围、批量操作与拖拽反馈（2026-07-17 第三批新增）

| ID | 需求/缺陷 | MVP | 状态 |
| --- | --- | --- | --- |
| REQ-FOLDER-008 | 进入文件夹后，资产画布包含全部后代文件夹中的资产（递归显示）；与 REQ-FOLDER-004 的范围显式切换共存，用户明确递归为最优先需求。 | 是 | 被 REQ-FOLDER-009 取代：默认不递归，显式开关后递归（2026-07-18） |
| REQ-FOLDER-009 | 文件夹递归显示改为**显式开关**（如范围栏「包含子文件夹」勾选），默认关闭只显示直接资产，勾选后才递归显示后代资产；「资源库根目录」与「所有资产」行为不变 | 是 | 已实现（2026-07-18）；人类验收通过（FOLDER-009） |
| REQ-FILTER-012 | 文件夹范围内搜索默认递归匹配全部后代文件夹中的资产（"理所当然"的默认行为，无需切换）。 | 是 | 已实现（2026-07-17：currentSearchScope 递归 + 孙级深度 worker 回归测试）；待人类验收（FILTER-012） |
| REQ-MENU-007 | 多选资产后，添加/移除标签等上下文操作批量作用于全部选中资产；逐项报告处理/跳过数量与原因。 | 是 | 已实现（2026-07-17：右键菜单批量既有 + Inspector 标签批量 + Inspector 评分批量 asset.rating.set + worker 逐项跳过报告齐全）；待人类验收（MENU-020、MENU-021）；收藏（favorite）批量未提需求，留作后续 |
| REQ-DND-003 | 拖拽中的资产预览图标缩小、透明度进一步降低，并使用圆角。 | 是 | 已实现（2026-07-17：setDragImage 自定义 96×72 预览、opacity 0.6、圆角 9px、多选计数徽标）；待人类验收（DND-003） |
| BUG-DND-001 | 拖拽资产悬停到文件夹上时目标高亮经常失效；悬停高亮必须稳定可靠。 | 是 | 已修复（2026-07-17：drop-target 特异性压过 hover、行子元素 pointer-events 豁免、dragleave relatedTarget 守卫、链接行补高亮）；待人类验收（DND-004） |

## 右键菜单语义基线

单资产菜单的默认分组方向：

1. 打开：默认应用打开、其他应用打开、在 Finder/Explorer 显示。
2. 剪贴板与组织：复制、粘贴、重命名、复制文件路径、移动到文件夹。
3. 元数据：标签、合集、AI 分析等当前上下文允许的操作。
4. 删除：移至回收站、永久删除；破坏性操作独立分组并确认。

多选菜单只显示对选择集合有定义的动作。混合 managed/linked/missing/trashed 时必须按固定快照计算目标，并明确说明跳过项。文件夹菜单复用相同的组、快捷键和危险状态，但调用独立的文件夹领域命令。

## 文件夹浏览模型约束

Renderer 需要消费显式的混合浏览项，而不是把目录伪造成资产：

```text
BrowseEntry = FolderBrowseEntry | AssetBrowseEntry

FolderBrowseEntry
  folder_id
  location_kind = managed | linked
  name
  status
  direct_asset_count
  recursive_asset_count
  child_folder_count
  cover_artifact_ids[]
```

封面选择、计数和后代范围由 Worker 批量计算。Renderer 不接收绝对路径，不逐卡查询数据库或文件系统。

## 暂定切片与依赖

| 暂定切片 | 范围 | 目标版本 | 依赖 |
| --- | --- | --- | --- |
| 0015 | UI 基础设施：中英文本地化、亮/暗主题 token、命令注册表与快捷键 | v0.1.0 | 可先行 |
| 0016 | 应用壳、资源库切换、历史导航、面包屑、浏览/发现工具栏 | v0.1.0 | 0015；部分依赖 0018 |
| 0017 | 文件夹卡片、封面、数量、递归范围、普通/链接统一树、文件夹/资产文件操作 | v0.1.0 | 0015 命令注册表；0002/0003/0007 |
| 0018 | 标签过滤与 Inspector chip 体验、Label 退役和数据兼容迁移 | v0.1.0 | 0015 本地化；0004/0005/0009 |
| 0019 | 产品正确性包：Inspector 缩略图/闪烁、组合键框选、瀑布流空当、图标与对齐 | v0.1.0 | 可与 0015 并行；0013/0014 |

建议执行顺序不是简单按编号串行：需求冻结后优先处理 0019 的核心缺陷；0015 与 0019 可并行；随后实施 0017/0018，再以稳定的命令、标签和文件夹模型完成 0016。0013 查看页面继续作为独立 MVP 核心 UX 收口。

## 2026-07-17 第二批反馈排期

产品负责人直接反馈，优先级高于常规池条目。按依赖关系与文件冲突分为三波（Wave 内轨道并行、Wave 间串行合流）：

| 波次 | 轨道 | 条目 | 说明 |
| --- | --- | --- | --- |
| Wave 1 | T1 视觉修饰包 | REQ-SELECT-003、REQ-NAV-005、REQ-THEME-004、REQ-CANVAS-006/007/008、REQ-SHELL-008/009/010 | 以 styles.css 与展示组件为主 |
| Wave 1 | T2 文件夹菜单命令 | REQ-MENU-006 | protocol→preload→main→worker 全链路，严格仿 2026-07-17 资产版 reveal/copy-path |
| Wave 1 | T3 回收站预览丢失 | BUG-TRASH-001 | 先定位根因（artifact/媒体解析对 trashed 状态的处理）再一次修到位 |
| Wave 1 | T4 文件夹原地编辑 | REQ-FOLDER-007 | 目录树 inline 新建/重命名，移除文件夹对话框及其装饰文案 |
| Wave 2 | T5 侧栏拖拽调宽 | REQ-SHELL-007 | 壳层布局状态 + 跨重启持久化 |
| Wave 2 | T6 拖拽移动/删除 | REQ-DND-001/002 | 依赖 T4 目录树稳定；worker 移动命令 |
| Wave 2 | T7 过滤增强包 | REQ-TAG-002（多标签）、REQ-FILTER-009（宽高比预设）、REQ-FILTER-010（分辨率预设） | 维度过滤方向的第一批落地 |
| Wave 3 | 终极目标 | 全面 UI/UX 审查与优化、功能性需求提案 | Wave 1/2 合流提交后执行 |

- REQ-FILTER-009：宽高比过滤提供常用预设（16:9 / 4:3 / 1:1 / 3:4 / 9:16），按最接近比例匹配，保留自定义输入。
- REQ-FILTER-010：分辨率过滤提供 1K / 2K / 4K 预设，可自定义输入。
- 文件夹「新建子文件夹/重命名」对话框的装饰文案（REQ-SHELL-009 范围内）随 T4 移除对话框一并解决，T1 不单独处理文件夹对话框。
- 每波完成后更新本文件、`docs/project-status.md`、人类验收清单并提交推送；Computer Use 不可用的环境按纪律记「未执行」并移交人工 QA。

**三波状态（2026-07-17 收口）**：Wave 1（T1–T4）与 Wave 2（T5–T7）已全部实现、合流并推送（见 project-status 与两份 Wave 开发日志）；Wave 3 第一步「全面 UI/UX 审查与提案」已完成——报告与 7 项待确认提案见 `docs/reviews/2026-07-17-wave3-ui-ux-audit.md`，截图证据 `docs/qa/evidence/wave3-ux-audit/`。提案待产品负责人拍板后按规则入池；REQ-CANVAS-003（工具栏迁出）建议按审查 P1 证据提前。

## 2026-07-17 Computer Use 真实操作审计发现

主 agent 用 kimi-cu Computer Use 插件对真实运行的 dev 应用做了 30+ 步真实鼠标键盘旅程（隔离 profile + 8 资产审计库），报告全文：`docs/reviews/2026-07-17-computer-use-ux-audit.md`。以下条目入池待排期，ID 沿用报告编号：

| ID | 问题 | 类别 |
| --- | --- | --- |
| CU-B1 | 移出合集后网格陷入「0 项 + 首次导入空态」，侧栏计数仍正确；需重新导航恢复 | 状态同步 bug |
| CU-B2 | 回收站恢复资产后侧栏计数与资源库信息面板计数不刷新 | 状态同步 bug |
| CU-B3 | 智能合集视图范围栏仍显示「所有资产 N 项」、侧栏选中态不跟随 | 状态同步 bug |
| CU-B4 | 资产右键「移出合集/加入合集」不按成员状态过滤，恒同时显示可点 | 菜单逻辑 bug |
| CU-B5 | 工具栏右侧溢出：1567px 窗口下导入 ZIP/浏览器扩展/后台任务/AI 设置宽度为 0 消失，无溢出菜单；关联 REQ-CANVAS-003 工具栏迁出 | UI bug |
| CU-B6 | 搜索无结果空态错误显示「这个文件夹还是空的」+ 导入按钮 | UX bug |
| CU-B7 | 回收站空态复用首次导入文案「把第一批素材放进来」 | UX bug |
| CU-M1 | 资源库下拉无「本机其他资源库/最近打开」列表（SHELL-004 实锤） | 功能缺失 |
| CU-M2 | 筛选面板无重置全部；超高被裁；Escape 不能关；无激活条件摘要 chip | 功能缺失 |
| CU-M3 | 文件夹右键菜单无「删除文件夹」项 | 功能缺失 |
| CU-M4 | 双击不打开查看页、右键无「查看」项，唯一入口 Enter 键不可发现 | 功能缺失 |
| CU-M5 | 智能合集无搜索条件也可保存（仅校验标题），产出「全部资产」合集 | 功能缺失 |
| CU-M6 | 智能合集侧栏项无计数（合集有，不一致） | 功能缺失 |
| CU-U1 | 递归视图中子文件夹资产无来源标识 | UX 建议 |
| CU-U2 | 搜索命中把分词文本额外显示为第二行「文件名」 | UX 建议 |
| CU-U3 | 多选时 Inspector 显示单资产详情而非 N 项汇总 | UX 建议 |
| CU-U4 | 无资源库时顶栏控件视觉上无 disabled 区分 | UX 建议 |
| CU-U5 | 合集/智能合集视图中导入按钮语义不明 | UX 建议 |
| CU-U6 | 「关闭资源库」按钮常驻资源库信息面板底部，有误触风险 | UX 建议 |

第二批（2026-07-17，真实用户库 142 资产/25 文件夹/5 级目录，全程只读）：报告全文 `docs/reviews/2026-07-17-computer-use-deep-audit-library2.md`。递归显示/递归搜索/宽高比预设/动图播放/批量提示等 12 项核心旅程验证通过，新发现如下：

| ID | 问题 | 类别 |
| --- | --- | --- |
| CU-D1 | 视频/gif 卡片无类型标识与时长角标，网格无法区分图片/视频/动图 | 功能缺失 |
| CU-D2 | 目录树无折叠交互（无 disclosure 箭头），25 文件夹全展开致合集/智能合集挤出首屏 | UX |
| CU-D3 | 搜索高亮上下文最多 3 行致卡片高度不一、与文件名重复（强化 CU-U2） | UX |
| CU-D4 | 搜索激活无「正在搜索: xxx」状态与一键清除，仅范围栏计数变化 | UX/状态 |
| CU-D5 | 过滤「格式」手填扩展名文本框，无格式 chips 与图片/视频/动图类型维度 | UX |
| CU-D6 | 筛选面板超高被裁且自身不可滚动，排序字段/方向不可达（实锤 CU-M2）；分辨率预设是否在裁剪区以下无法确认 | UI bug |
| CU-D7 | 疑似 bug：gif 缩略图黑帧（2/2 样本网格纯黑、查看页首帧彩色），需开发核实提取帧时机 | 疑似缺陷 |
| CU-D8 | gif 无暂停/逐帧控制；gif 元数据无时长/帧数（视频有） | 功能缺口 |
| CU-D9 | 深层长文件夹名折行撑高行（明日方舟排版参考），无省略或自适应 | UX |

根因假设（B1–B3 同族）：数据变更/导航后部分 UI 订阅者未被失效通知，建议统一 invalidation 总线。拖拽类问题（BUG-DND-001、DND-003）因 CU 合成拖拽对 HTML5 DnD 无效，仍移交 Playwright/人工。

## 2026-07-17 第三批反馈（验收驱动）

用户在真实库上逐项验收后的结论与新需求。验收状态变化见 `docs/qa/human-acceptance-checklist.md`（SHELL-009 / LIB-003 / MENU-020 / FILTER-012 通过；FOLDER-001 不通过）。以下条目入池待排期：

| ID | 需求/缺陷 | 用户原话要点 | 类别 |
| --- | --- | --- | --- |
| REQ-SELECT-004 | **UE 式多选属性编辑模型**（替代当前“统一批量应用”提示）：标量属性（描述/评分/喜欢/源链接等）值不一致时显示「多个值」且控件禁用；值一致（含同为空）时可编辑并应用到全部选中。标签为特殊类型：显示所有选中项的**共有标签（交集）**，共有标签可一键移除（从全部选中移除），也可添加新标签（为全部选中添加）。原则：UX 为易用性服务。**状态**：已实现（2026-07-18，`Serpent-eb7`）；待人类验收 SELECT-009/010 | “类似 UE 多选 Actor 后统一修改同类参数；a 值不同显示‘多个值’且无法修改；b 值相同可改并应用到 AB；c 标签显示共有标签，共有可一起删，新标签同时为 AB 添加” | 交互重构 |
| REQ-FOLDER-009 | 文件夹递归显示改为**显式开关**（如范围栏「包含子文件夹」勾选），默认关闭只显示直接资产，勾选后才递归显示后代资产；「资源库根目录」与「所有资产」行为不变。**状态**：已实现（2026-07-18）；人类验收通过（FOLDER-009） | “递归显示不通过。需要显式勾选选项才能够显示递归显示内容。” | 交互修改（FOLDER-001 验收不通过的修复路径） |
| REQ-VIEW-002 | 查看页打开即直接显示原图/原视频（原分辨率无压缩），不以「正在生成预览」+ 加载图标阻塞查看；预览/转码生成退为后台增强，已有缓存直出 | “查看就是查看，原分辨率无压缩地查看。” | 性能/交互 |
| REQ-VIEW-003 | 视频元数据扩展：帧率、码率，以及编码、音轨等；查看页与 Inspector 展示 | “视频可以显示更多元数据，比如帧率、码率等等” | 功能 |
| REQ-VIEW-004 | 查看页重新定位为**资产浏览界面的附属层**：不得以覆盖整窗压住壳层菜单的方式实现；后退/前进先退出查看页返回浏览；切换文件夹/范围时自动退出查看页并显示新范围；查看页不进入浏览历史栈（澄清队列 #12 按此裁决）。2026-07-18 补充：叠层导致挡资源库设置菜单、切文件夹后仍挡在最前——需深层修复挂载/生命周期，禁止只调 z-index。工单 `Serpent-ts2`。**2026-07-18 人类验收通过**（`VIEWER-004`，基线 `34442b0`）。 | “查看页面应该设计为资产浏览界面的附属界面，可以正常退出，切换其他文件夹时也可以正常退出查看界面” | 信息架构 |
| REQ-VIEW-006 | 查看页适配与控件：打开时最长边撑满窗口（contain）；底部缩放条缩小、加减改为滑块、文案改为 Fit；上一/下一与浏览面板当前结果序一致；去掉查看顶栏。工单 `Serpent-3w8`（依赖 ts2）。**2026-07-18 人类验收通过**（`VIEWER-005`–`007`，基线 `34442b0`）。 | 2026-07-18 双击查看反馈 | UX |
| REQ-VIEW-005 | 视频播放器增强：空格播放/暂停、倍速控制、可拖拽进度；评估更换更好用的播放器实现 | “换一个更好用的视频播放器，支持空格暂停，最好支持倍速” | 功能 |
| REQ-CANVAS-009 | 网格卡片动图/视频预览：悬停（或选中）时播放 gif 动图与视频预览，静止时显示封面 | “浏览界面动图、视频的预览” | 功能 |
| BUG-VIEWER-001 | 查看页视频提前循环：约 5 秒视频只播放约 2 秒即开始循环重复（时长/循环点错误，需定位根因） | “明明有一个5秒钟的视频，双击查看，只播放2秒钟就开始循环重复播放了” | 已修复（2026-07-18：ready 后轮询重置 directApproved；见 VIEWER-003） |
| REQ-SHELL-011 | 侧栏拖到小于阈值宽度时自动隐藏；隐藏后从对应屏幕边缘拖出恢复（SHELL-009 已验收的增强） | “拖动到一个较小的大小之后，可直接隐藏。隐藏之后也可以拖动对应的边缘拖出来” | 交互增强 |
| REQ-SHELL-012 | 创建资源库流程文案正式化，去除口语化表达 | “创建资源库的UI有点太口语化了，能不能正常一点” | 文案 |
| REQ-SHELL-013 | 所有可点击图标 hover 显示功能提示（tooltip）：纯图标按钮当前无任何说明，用户看不懂侧栏「添加文件夹」「导入链接文件夹」图标含义；全壳层排查纯图标控件，统一补齐 tooltip（及无障碍 aria 标签）。**状态**：已实现（2026-07-18）；人类验收通过（SHELL-013） | “任何可点击的图标，再hover时都需要显示提示。例如说添加文件夹和链接文件夹，我就没看懂是啥意思” | UX |
| REQ-SHELL-014 | 禁止「中文 + 括注英文/术语」冗余标签：UI 标签只保留一种明确表达，不叠加括注。已定位实例：`AiConfigDialog.tsx`「AI 配置 (BYOK)」「标签 (Tags)」、`InspectorPanel.tsx`「源链接 (URL)」「色卡 (Palette)」、过滤面板「宽度/高度/长边 (px)」；全局排查同类模式后统一清理（纯单位括注可去括号或直接去掉） | “UI标签禁止出现冗余文字，比如说有个界面有‘标签（Tags）’、‘AI配置（BYOK）’，非常的冗余、非常的迷惑” | 文案/UX |
| CU-M3（升级） | 文件夹删除功能（右键菜单无「删除文件夹」）——用户第三批再次点名“文件夹的删除功能呢”，优先级提升，排入下一增量；删除语义按澄清队列 #7 裁决 | “文件夹的删除功能呢” | 功能缺失 |
| REQ-FILTER-001/002（升级） | Eagle 式紧凑维度过滤条替代大面板长表单：2026-07-16 已入池但一直停留「待原型」，期间 Wave 2 仍在旧面板内追加宽高比/分辨率预设——2026-07-18 用户再次点名，提升为下一增量高优先级；旧面板查询语义保留（REQ-FILTER-002），交互层整体替换为维度过滤条 + 已启用条件工具栏 chips，参考 `docs/前端参考/2026-07-16-eagle-filter-popover.png` | “过滤换成eagle这种样式，而非一个大面板，填写所有过滤条件” | 交互重构 |

关联说明：REQ-VIEW-004 同时覆盖用户在 NAV-002 验收中补充的“进入查看页后怎么后退都在查看页”问题；REQ-FOLDER-009 与 COLLECTION-007（合集“包含子合集”开关）交互模式对齐。

## 2026-07-18 第四批反馈（beads 开单）

用户反馈经拆分去重后直接进入 beads 工单系统；本表只做映射备查，执行状态以 bd 为准（`bd ready --json`）。

| 反馈要点 | 处置 | 工单 |
| --- | --- | --- |
| 网格动图/视频预览；视频 2 秒循环；侧栏拖小隐藏；文件夹删除；播放器空格/倍速；创建资源库文案口语化；图标 hover 提示；中英括注冗余 | 重复，已在池 | Serpent-05o / ond / 4gk / ekj / 2j9 / rxx / d3c / c1p |
| 排序提升为一等模式（名称/修改日期/大小/分辨率/时长） | 新开 P2 | Serpent-w4p |
| 分类（分组）模式：先分类再排序，维度含修改日期/作者/网址/主色调/长宽比 | 新开 P4（MVP 后） | Serpent-84m |
| 「作者」元数据 | 新开 P2 | Serpent-7x0 |
| 平铺改 justified 行布局（参考图 `docs/前端参考/2026-07-18-tile-layout-reference.png`） | 新开 P2 | Serpent-8nj |
| 「可用」不显示图标，仅「不可用」显示断联表现 | 已实现（2026-07-18，见 AVAIL-001） | Serpent-6nb |
| 删除 Inspector 底部资源库路径与「关闭资源库」按钮 | 已实现（2026-07-18，见 INSPECT-006） | Serpent-t4c |
| 侧栏展开/折叠成套图标+左折叠图标移到前进/后退之前 | 新开 P2 | Serpent-6ey |
| macOS 红绿灯内嵌工具行最左（无边框顶栏） | 新开 P2 | Serpent-4ze |
| 色卡复制 hex 弹确认提示；删除自定义色卡；拖拽复制模式 | 新开 P3 | Serpent-woc / 7pg / aa3 |
| 「标签文字都需要书面」 | 范围补注进 REQ-SHELL-014 | Serpent-c1p |

## 集中澄清队列

以下问题尚未由产品负责人确认，后续 agent 不得自行猜测；等本轮反馈结束后一次性提问：

1. 左上角“按钮”的完整需求文字。
2. 文件夹显示的数量是直接资产数、递归资产数，还是同时显示两者。
3. 文件夹封面从直接资产还是所有后代资产选图，以及排序规则。
4. 文件夹卡片是否参与与资产相同的框选和多选操作。
5. “复制/粘贴”指 Serpent 内部资产/文件夹复制，还是操作系统文件剪贴板，或两者都要。
6. 文件夹右键菜单反馈末尾“删除、文件夹”是否遗漏“在文件浏览器中打开”等动作。
7. 正常视图中的“删除文件”是否等同永久删除源文件，以及 managed/linked 的确认差异。
8. 不设置标签管理页面后，标签重命名和全局删除从过滤 popover、Inspector 还是其他入口进入。
9. ~~旧 Label 值如何迁移。~~ 已确认：v0.1.0 发布前测试数据直接丢弃，不映射为其他概念；含 Label 条件的智能合集删除。
10. 框选修饰键的精确集合运算：替换、并集、差集、对称差，以及 Shift/Ctrl(Command)+Shift 的定义。
11. 主题默认跟随系统还是沿用暗色；语言默认跟随系统还是首次选择。
12. ~~后退/前进记录哪些状态：文件夹/合集/搜索/过滤/滚动位置，以及查看页面是否进入历史栈。~~ 已裁决（2026-07-17 第三批反馈）：查看页是资产浏览界面的附属层，不进入浏览历史栈；后退/前进先退出查看页；切换文件夹/范围时自动退出查看页（REQ-VIEW-004）。
13. 资产重命名是否同样从对话框改为画布内原地编辑（扩展名保留如何呈现）？文件夹已按第二批反馈确定为原地编辑（REQ-FOLDER-007）。

## 验收与证据要求

- 每个切片先完成可操作原型或真实应用路径，再进入实现；Eagle 只作为方向参考。
- 0015–0019 的新增功能在当前合流状态完成相关自动化、Computer Use 和截图证据前，不得进入人类待验收队列。
- 图片预览必须验证 `complete && naturalWidth > 0`；文件夹封面和 Inspector 缩略图也适用。
- 主题至少截图检查亮/暗两套的应用壳、画布、右键菜单、Inspector、空/加载/错误状态。
- 本地化至少检查中英文长文本溢出、快捷键标签、错误原因和重启持久化。
- 小卡片/瀑布流必须覆盖多种窗口宽度和 96/160/320 卡片尺寸，证明首尾可达且横向空间合理利用。

## 视觉参考

- `docs/前端参考/2026-07-16-eagle-layout-annotated.png`：Eagle 信息架构、资源库入口、过滤工具栏和 Inspector 密度方向。
- `docs/前端参考/2026-07-16-eagle-filter-popover.png`：形状/长宽比维度式过滤 popover。
- `docs/前端参考/2026-07-16-serpent-masonry-gap.png`：Serpent 小卡片/瀑布流右侧大面积空当缺陷。
- `docs/前端参考/2026-07-16-tag-chip-reference.png`：Inspector 标签 chip 的交互方向参考；视觉样式仍需按 Serpent 统一设计系统重做。
