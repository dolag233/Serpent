# 2026-09-03 PR #16 / #17 审查与接入记录

## 审查结论

- [PR #16](https://github.com/dolag233/Serpent/pull/16)（文件夹 disclosure 缩进）未发现 P0/P1；改动只涉及导航行布局和对应单测，建议接入。保留项是需要真实 Electron 检查深层目录、链接文件夹和行内编辑态的几何位置。
- [PR #17](https://github.com/dolag233/Serpent/pull/17)（文件夹搜索递归）方向符合当前 backlog，但原实现存在 P1：搜索期间切换“包含子文件夹”会把活动搜索变成非递归；产品简报也与 backlog 的搜索语义冲突。

## 接入处理

- PR #16 已在 `dev` 以 merge commit 接入。
- PR #17 已在 `dev` 以 merge commit 接入。
- 同步修正 App 搜索期间的范围计算：有文本搜索时始终使用递归 folder scope；无搜索时继续遵循“包含子文件夹”开关。
- 补充搜索期间切换开关的 Electron E2E 断言，并同步 `docs/product-brief.md` 的产品语义。

## 评审保留项

PR #16 的测试主要验证 DOM 内联样式，未替代真实窗口几何证据；PR #17 的链接文件夹虚拟子目录递归和大库性能仍需要独立专项证据。两项均不能仅凭单测写成完整人类验收通过。
