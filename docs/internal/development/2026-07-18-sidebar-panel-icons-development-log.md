# 2026-07-18 侧栏面板成套图标与左折叠移位（Serpent-6ey）

## 范围

侧边栏展开/折叠改用同一套面板语义图标（开/关两态）；左侧折叠按钮移到前进/后退之前。

## 实现

- 新增 `panel-left` / `panel-left-close` / `panel-right` / `panel-right-close`
- 顶栏左：折叠 → 后退/前进 → 资源库切换器
- 右栏折叠与浮现按钮共用右侧面板图标对
- 壳层 E2E：断言折叠按钮在后退按钮左侧

## 验收

人类清单 **SHELL-014**。
