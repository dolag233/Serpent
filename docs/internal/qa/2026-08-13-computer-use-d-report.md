# 2026-08-13 Computer Use 人工验收（D 阶段）

## 验收对象

- 当前 HEAD packaged 应用：`out/Serpent-darwin-arm64/Serpent.app`
- 资源库：隔离测试库
- 启动状态：已打开，界面显示 717 项资产、185 个文件夹

## 通过的人工路径

- 主界面和资源库恢复：通过
- 搜索资产并显示 Inspector：通过
- 图片查看页：通过；查看页显示缩放、色彩空间、旋转、适应、全屏和关闭控件
- 预览旋转后关闭查看页：通过
- 合集范围切换：通过；可进入「B站评论区精选」，包含子合集树和空合集提示
- 文件夹范围切换：通过；可进入嵌套文件夹样本，显示子资产/子文件夹结构
- 回收站范围：通过；显示 423 项，展示恢复期限和原始文件夹路径
- 通用设置：通过打开；常规分类可见
- MCP 设置：通过打开；显示运行状态、回环地址、自动启动、客户端凭据与访问模式

## 阻断与限制

- 旧安装包 `/Applications/Serpent.app` 启动时显示 “Serpent could not start”。该包不是当前 HEAD，未作为当前代码验收证据；当前 HEAD packaged 应用已单独启动成功。
- 本轮未执行永久删除、清空回收站、修改 MCP 权限、导入外部文件等会改变用户数据或权限的动作；这些路径保留给需要专门确认的人工验收。
- 当前环境没有 Windows，因此 Windows UI、托盘和 GPU/WebGL 未人工验证。
