# 切片 0004 双轴代码审查

> 状态：Spec 未通过；最小主线已验证
> 日期：2026-07-13

## 审查范围

- 固定范围：`8dc2470...cdc2247`
- 规格：`docs/implementation/0004-tags-collections-metadata-vertical-slice.md`
- 补充复审：当前 working tree 中的公共 UI、协议版本修复和
  `tests/e2e/organization-search-trash.test.ts`

固定范围用于避免审查基线漂移；working-tree 复审仅用于确认固定范围之后的 UI 可达性修复，
不能被表述为已经提交的历史。

## Standards 轴

- 通过：Renderer 只调用语义 API；数据库所有权仍在 Library Worker；协议由 Zod 校验。
- 通过：标签分配幂等、合集成员关系和元数据写入均由 Worker 事务/约束保护。
- 已修复：初始元数据版本为 0，而请求与响应 schema 曾要求最小 1，导致真实 UI 首次保存失败。
- 通过：新增 E2E 通过公共 UI 操作，不直连数据库或注入内部状态。
- 通过：资产位置类型来自 Worker 的 `AssetSummary.locationKind`；全部资产、标签、合集和搜索
  视图不会再把链接资产误当托管资产删除。
- 已修复：递归父合集中的子合集成员不再显示虚假“已移除”结果；UI 先确认直接成员关系并
  指引用户进入对应子合集。
- 非阻断风险：本轮开发日志为事后重建，未遵循“规格→实现→同步日志→审查→QA”的完整时序。

## Spec 轴

- 通过：标签创建与资产分配、合集创建与成员加入、Label/评分/喜欢编辑均有公共 UI 证据。
- 通过：乐观锁版本从 0 开始并在连续写入中递增，未静默覆盖。
- Worker 测试为 CRUD、级联、递归合集、排序和元数据边界提供实现证据。
- 通过：标签/合集重命名删除、合集成员移除已有公共 UI E2E。
- 待验证：合集树拖拽/重排、重启持久化、并发冲突 UI、人工色卡。

## 结论

Standards 主线未发现阻断项，但 Spec 仍缺标签批量移除、完整合集树编辑与重排、成员排序、
智能合集编辑删除和人工色卡编辑。结论为 Spec 未通过；当前整理主线 E2E 通过不等于
切片完成。Windows 与 macOS 人工视觉 QA 仍未验证。
