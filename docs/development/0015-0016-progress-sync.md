# 0015–0016 进度同步记录

> 更新时间：2026-07-16
> 当前集成基线：`07d2f7e`
> 目的：把前置 agent 已经落地的 UI/UX 增量同步到项目状态、需求池和人类验收队列，避免只看最近一个大提交时遗漏功能。

## 已进入当前主线的实现

| 提交 | 已落地内容 | 当前判定 |
|---|---|---|
| `38fa873` | 画布网格装饰移除；右下角 `SERPENT / LOCAL WORKSPACE` 移除；导航“资源导航”和连接状态页脚移除；Inspector 面板标题移除；滑块保留无障碍名称但移除“缩略图大小”冗余文字。 | 功能已实现，待人类视觉验收；不等于整个应用壳完成。 |
| `591f524` | 顶部资源库/范围显示、部分工具栏垂直对齐、链接文件夹彩色图标。 | 部分实现：真实可点击多级面包屑、后退/前进、普通/链接文件夹合并尚未完成。 |
| `64521c3` | Inspector 紧凑布局、tag chip、工具栏 CSS 顺序调整。 | 已被 `5b8b8fe` 的 Label 退役、真实预览和标签交互进一步收口；标签与预览已有独立验收记录。 |
| `197ea9e` | 资产/合集/标签相关右键菜单按“打开/组织/元数据/删除”等语义分组并用分隔区分。 | 功能已实现，待人类验收；菜单完整动作集合仍是 0017 范围。 |
| `e2d5d60` | 菜单项显示快捷键；快捷键匹配逻辑随后由 `5b8b8fe` 的 `asset-command-shortcuts.ts` 统一。 | 当前两个快捷键路径有实现和测试；统一命令注册表及完整菜单命令仍未完成。 |
| `5b8b8fe` / `07d2f7e` | Label 退役、Inspector 真实预览/标签、瀑布流、框选和菜单输入模式的集成实现、审查与 QA 文档。 | 见 0018–0019 QA 和人类验收清单。 |

## 仍不能宣称完成的范围

- 左上角仍保留 `S` 品牌字形，资源库下拉菜单尚未承载资源库切换和操作。
- 当前顶部范围显示仍是静态“资源库 > 当前范围”表达，不是可点击父目录链；工作区后退/前进尚未完成。
- 普通文件夹和链接文件夹仍分区，链接 tooltip/离线红色断链状态和统一目录树尚未完成。
- 浏览工具栏仍有导入/导出等常驻动作，Eagle 风格过滤条尚未完成。
- 单资产完整文件菜单、文件夹菜单、复制/粘贴/重命名/复制路径等领域命令尚未完成。
- 菜单仍可能枚举标签/合集，批量可搜索二级选择器和统一命令注册表尚未完成。

## 本次同步后的下一排期

1. 0016-A：应用壳与导航信息架构（资源库菜单、可点击面包屑、历史导航、普通/链接统一树）。
2. 0015：统一命令注册表、中英文和亮/暗主题 token。
3. 0017：文件夹卡片/封面/计数/递归范围及完整资产/文件夹文件操作。
4. 0018 后续：标签过滤入口和批量可搜索选择器。

本记录只同步事实，不把“代码存在”直接写成“人类验收通过”；可验收状态以 [人类验收清单](../qa/human-acceptance-checklist.md) 为准。
